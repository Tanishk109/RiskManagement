# Web components

The current four-module product is intentionally small and implemented in the root vinext adapter. Extract a component here only when it has a second consumer or independent test surface.
