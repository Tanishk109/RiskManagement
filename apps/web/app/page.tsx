// The deployable Sites adapter remains at the repository root because vinext
// requires a root app directory. This re-export keeps the requested monorepo
// surface discoverable without maintaining a second dashboard implementation.
export { default } from "../../../app/page";
