# Failure Analysis

**Not evaluated yet.** Run `make error-analysis` after the validation model exists.

The generated report will inspect false positives, false negatives, true positives, and true negatives across supported groups such as product category, identity missingness, amount bands, and later temporal regions. Rule proposals remain disabled until this analysis provides evidence.
