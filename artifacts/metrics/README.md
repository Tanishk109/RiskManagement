# Metrics artifacts

Generated validation and held-out metrics are written here by the ML scripts. No metric file exists in this checkout because the real IEEE-CIS data has not been supplied.

`final_test_metrics.json` is the only product source of truth after final evaluation. `final_test_predictions.csv` contains protected row-level outputs and is ignored by git.
